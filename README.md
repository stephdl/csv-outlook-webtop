# csv-outlook-webtop
 ￼  ￼  ￼   Claude responded: Outlook contacts CSV to WebTop-ready CSV converter — handles Windows-1252 and UTF-8 BOM encodings, zero manual field mapping on import. Outlook contacts CSV to WebTop-ready CSV converter — handles Windows-1252 and UTF-8 BOM encodings, zero manual field mapping on import.
